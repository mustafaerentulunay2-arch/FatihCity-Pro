import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {useAuth} from '../contexts/AuthContext';import {users} from '../data/mock';
export default function Login(){const [email,setEmail]=useState(users[0].email);const {login}=useAuth();const nav=useNavigate();return <div className="login"><div className="login-card"><h1>FatihCity Pro</h1><p className="muted">Demo kullanıcı seç ve panele giriş yap.</p><select value={email} onChange={e=>setEmail(e.target.value)}>{users.map(u=><option key={u.id} value={u.email}>{u.name} - {u.role}</option>)}</select><br/><br/><button className="btn" style={{width:'100%'}} onClick={()=>{login(email);nav('/dashboard')}}>Giriş Yap</button></div></div>}
